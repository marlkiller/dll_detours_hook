# This is a .NET project (C#), not a native C++ project.
# Use build.cmd or "dotnet build" directly.
# This CMakeLists.txt exists only to prevent CMake errors if this directory is
# accidentally picked up by parent CMake projects.
cmake_minimum_required(VERSION 3.28)
project(DotnetHarmonyHook)
message(STATUS "DotnetHarmonyHook is a .NET project. Use build.cmd or 'dotnet build' to compile.")
